services:
  visualsubnetcalc:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visualsubnetcalc
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "80:80"
      - "8080:8080"
